type User {
    id: ID!
    name: String!
    username: String!
    email: String
    phone: String
    website: String
    createAt: String!
    updateAt: String!
}

extend type Query {
    getUser(id: ID): User
    allUser: [User]
}